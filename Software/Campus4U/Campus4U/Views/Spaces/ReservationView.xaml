<Window x:Class="Client.Presentation.Views.Spaces.ReservationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        mc:Ignorable="d" 
        Title="ReservationView" Height="700" Width="450" Background="White"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Naziv}" 
           FontSize="32" FontWeight="Bold" Margin="0,0,0,20"/>

        <Border Grid.Row="1" CornerRadius="20" ClipToBounds="True" Height="220" Margin="0,0,0,30">
            <Image Source="{Binding SlikaPutanja}" Stretch="UniformToFill"/>
        </Border>

        <StackPanel Grid.Row="2">
            <Border BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="15" Padding="10,5" Margin="0,0,0,15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="📅" VerticalAlignment="Center" Margin="5,0,10,0"/>
                    <DatePicker Grid.Column="1" BorderThickness="0" VerticalAlignment="Center" 
                        Background="Transparent" Name="Datum"/>
                </Grid>
            </Border>

            <Grid Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="15" Padding="10,5">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🕒" VerticalAlignment="Center" Margin="5,0,10,0"/>
                        <ComboBox Text="09:00" BorderThickness="0" VerticalAlignment="Center" Width="80" Name="odVrijemeCombo" IsEditable="false"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="15" Padding="10,5">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🕒" VerticalAlignment="Center" Margin="5,0,10,0"/>
                        <ComboBox Text="10:00" BorderThickness="0" VerticalAlignment="Center" Width="80" Name="doVrijemeCombo" IsEditable="false"/>
                    </StackPanel>
                </Border>
            </Grid>

            <Border BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="15" Padding="15">
                <Grid>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                        <TextBlock Text="👥" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBlock Text="OSOBE" FontWeight="Bold" VerticalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button x:Name="BtnMinus" Content="-" Width="35" Click="BtnMinus_Click" Cursor="Hand"/>
                        <TextBlock x:Name="TxtBrojOsoba" Text="1" Margin="15,0" FontWeight="Bold" VerticalAlignment="Center"/>
                        <Button x:Name="BtnPlus" Content="+" Width="35" Click="BtnPlus_Click" Cursor="Hand"/>
                    </StackPanel>
                </Grid>
            </Border>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button x:Name="BtnDodajUFavorite" Style="{StaticResource SecondaryButton}" Height="35" Width="146" HorizontalAlignment="Right" Margin="0,15,0,0" Click="BtnDodajUFavorite_Click">
                    Dodaj u favorite
                </Button>
                <Button x:Name="BtnUkloniIzFavorita" Style="{StaticResource SecondaryButton}" Height="35" Width="146" HorizontalAlignment="Right" Margin="0,15,0,0" Click="BtnUkloniIzFavorita_Click" Visibility="Collapsed">
                    Ukloni iz favorita
                </Button>
            </StackPanel>
        </StackPanel>

        <Grid Grid.Row="3" Margin="0,20,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="Natrag" Height="50" Background="White" Foreground="#7A0C19" BorderBrush="#7A0C19" FontWeight="Bold" Click="BtnNatrag_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="15"/>
                    </Style>
                </Button.Resources>
            </Button>

            <Button Grid.Column="2" Content="Rezerviraj" Height="50" Background="#7A0C19" Foreground="White" BorderThickness="0" FontWeight="Bold" x:Name="BtnRezerviraj" Click="BtnRezerviraj_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="15"/>
                    </Style>
                </Button.Resources>
            </Button>
        </Grid>
    </Grid>
</Window>